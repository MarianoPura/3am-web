{
    "name": "3am/website",
    "description": "3AM Media, Technology and Ventures Inc. — corporate website",
    "type": "project",
    "license": "proprietary",
    "require": {
        "php": ">=8.2",
        "ext-pdo": "*",
        "ext-pdo_mysql": "*",
        "ext-mbstring": "*",
        "ext-json": "*",
        "ext-fileinfo": "*",
        "ext-openssl": "*",
        "ext-curl": "*",
        "vlucas/phpdotenv": "^5.6",
        "aws/aws-sdk-php": "^3.300",
        "phpmailer/phpmailer": "^6.9",
        "robthree/twofactorauth": "^3.0",
        "erusev/parsedown": "^1.7",
        "monolog/monolog": "^3.5"
    },
    "require-dev": {
        "phpunit/phpunit": "^11.0",
        "squizlabs/php_codesniffer": "^3.9",
        "phpstan/phpstan": "^1.10"
    },
    "suggest": {
        "ext-imagick": "Strongly preferred over GD for the media pipeline — AVIF support and reliable EXIF/payload stripping.",
        "ext-exif": "Reading orientation before re-encoding uploaded images.",
        "ext-intl": "Locale-aware date and number formatting."
    },
    "autoload": {
        "psr-4": {
            "App\\": "app/"
        },
        "files": [
            "app/Helpers/functions.php"
        ]
    },
    "autoload-dev": {
        "psr-4": {
            "Tests\\": "tests/"
        }
    },
    "config": {
        "optimize-autoloader": true,
        "sort-packages": true,
        "platform": {
            "php": "8.2.31"
        }
    },
    "scripts": {
        "migrate": "php bin/migrate.php",
        "lint": "phpcs --standard=PSR12 app/ config/ bin/",
        "analyse": "phpstan analyse app/ --level=6",
        "test": "phpunit"
    },
    "comments": {
        "aws-sdk": "Trimmed to the S3 client at deploy time — see bin/deploy.sh. The full SDK is ~50MB of vendor code we do not ship.",
        "platform-php": "Pinned to the server's 8.2.31. Code is written 8.2-to-8.4 clean; PHP 8.2 security support ends 2026-12-31 and the interpreter upgrade is scheduled post-launch."
    }
}
